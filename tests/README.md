# Tests

This directory is a minimal Phase 0 scaffold for future deterministic adapter tests.

No behavior tests are added in Phase 0. Future phases should add tests here without mutating runtime behavior or modifying `third_party/PyAIPilotExample/**`.
